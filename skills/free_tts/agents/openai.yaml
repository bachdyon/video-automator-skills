interface:
  display_name: "Free TTS"
  short_description: "Generate free/local Vietnamese narration with VieNeu-TTS"
  default_prompt: "Use $free-tts to automatically generate Vietnamese narration with VieNeu-TTS when AusyncLab API credentials are unavailable."

policy:
  allow_implicit_invocation: true
